import Head from 'next/head' 
import { Inter } from 'next/font/google'   
import Footer from '@/components/ContractAddress/Footer' 
import Hero from '@/components/ContractAddress/Hero'
import TreasureboxNav from '@/components/ContractAddress/Navbars'
import CallAction from '@/components/ContractAddress/CallAction'

const inter = Inter({ subsets: ['latin'] })

const treasurebox = () => {
    return (
        <>
        <Head>
        <title>Myntist Treasurebox</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="treasurebox-wrapper"> 
        <TreasureboxNav/>
        <Hero/>
        <CallAction/>
        <Footer/>
      </main>
    </>
    )
}

export default treasurebox